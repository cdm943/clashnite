using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PruebaPreco : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("Prueba Preco");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
